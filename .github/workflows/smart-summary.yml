name: Smart Summary Generator

on:
  schedule:
    - cron: '0 */6 * * *'  # Alle 6 Stunden
  workflow_dispatch:  # Manuell ausführbar

jobs:
  update-deals:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Run scraper
        run: |
          node power-scraper.js
          
      - name: Commit and push if changed
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add deals.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Auto-update deals [$(date +'%Y-%m-%d %H:%M')]" && git push)
```

### 2. **Überprüfe deine Dateistruktur**

So sollte es aussehen:
```
dein-projekt/
├── .github/
│   └── workflows/
│       └── smart-summary.yml  ← YAML Workflow
├── power-scraper.js           ← JavaScript Scraper
└── deals.json                 ← JSON Daten
